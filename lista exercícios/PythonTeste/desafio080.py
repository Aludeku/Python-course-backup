valores = []
for v in range(0,5):
    adc = valores.append(int(input('Digite um valor: ')))
    count = adc
    if adc <= count:
        adc = valores.insert(adc, 0)

print(f'Você digitou os valores {valores}')